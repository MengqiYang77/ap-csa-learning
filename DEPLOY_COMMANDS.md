# 🚀 部署命令 - 复制粘贴即可

## ✅ 第一步：配置已完成

我已经帮你修改了 `vite.config.ts`，添加了正确的 base 路径：
```typescript
base: '/ap-csa-learning/',
```

## 📦 第二步：在本地终端运行这些命令

**重要提示：** 
- 你需要在**本地电脑**的项目文件夹中运行这些命令
- 不是在 Manus 网页中运行
- 需要先下载项目文件到本地

### 下载项目文件

1. 点击 Manus 界面右侧的 **Code** 按钮
2. 点击 **Download all files** 下载整个项目
3. 解压到你的电脑上
4. 用终端（Terminal）或命令提示符（CMD）打开这个文件夹

### 运行部署命令

打开终端后，**依次复制粘贴**以下命令：

#### 1. 初始化 Git 仓库
```bash
git init
```

#### 2. 配置 Git（如果还没配置过）
```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
```

#### 3. 添加所有文件
```bash
git add .
```

#### 4. 提交代码
```bash
git commit -m "Initial commit: AP CSA Encapsulation Learning Platform"
```

#### 5. 添加远程仓库
```bash
git remote add origin https://github.com/MengxiYang77/ap-csa-learning.git
```

#### 6. 推送到 GitHub
```bash
git branch -M main
git push -u origin main
```

## 🔐 如果推送时要求登录

GitHub 不再支持密码登录，你需要使用 **Personal Access Token**：

### 创建 Token：

1. 访问：https://github.com/settings/tokens
2. 点击 **Generate new token (classic)**
3. 勾选 **repo** 权限
4. 点击 **Generate token**
5. **复制 token**（只显示一次！）

### 使用 Token：

推送时输入：
- Username: `MengxiYang77`
- Password: `粘贴你的 token`

## ⚙️ 第三步：启用 GitHub Pages

1. 进入你的仓库：https://github.com/MengxiYang77/ap-csa-learning
2. 点击 **Settings**（设置）
3. 左侧菜单找到 **Pages**
4. 在 **Source** 下拉菜单选择 **GitHub Actions**
5. 等待 2-3 分钟

## 🎉 完成！

你的网站将在这个地址上线：

**https://mengxiyang77.github.io/ap-csa-learning/**

## 🔍 查看部署状态

1. 进入仓库主页
2. 点击顶部的 **Actions** 标签
3. 查看工作流运行状态
4. 绿色 ✓ = 部署成功
5. 红色 ✗ = 部署失败（查看错误信息）

## 🔄 以后更新网站

每次修改代码后，只需运行：

```bash
git add .
git commit -m "描述你的更改"
git push
```

网站会自动更新！

## ❓ 常见问题

### Q: 推送失败，提示 "Permission denied"
**A**: 检查你的 Personal Access Token 是否正确，是否有 repo 权限

### Q: 网站显示 404
**A**: 
1. 确认 GitHub Pages 已启用
2. 检查 `vite.config.ts` 中的 `base` 是否为 `/ap-csa-learning/`
3. 等待几分钟让部署完成

### Q: 样式丢失
**A**: 确保 `base` 配置正确，前后都有斜杠 `/`

### Q: 如何查看部署日志
**A**: 
1. 进入仓库的 **Actions** 标签
2. 点击最新的工作流
3. 展开步骤查看详细日志

## 📞 需要帮助？

如果遇到问题：
1. 检查 Actions 标签中的错误信息
2. 确认所有命令都成功执行
3. 查看 `docs/GITHUB_DEPLOYMENT.md` 获取更多帮助

---

**祝你部署顺利！🎉**
